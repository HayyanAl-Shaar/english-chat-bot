import React, { type ReactNode } from 'react'

const Header = () => {
  return (
    <div className="header">
        <button className="headerButton">Browse</button>
        <button className="headerButton">About</button>
    </div>
  )
}

export default Header